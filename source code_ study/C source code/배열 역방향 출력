#include <stdio.h>

int main(void)
{
	int arr[100];
	int k = 0;
	for (int i = 99; i >= 0; i--)
	{
		while (1)
		{
			arr[i] = k;
			k++;
			break;
		}
	}
	for (int i = 0; i <= 99; i++)
	{
		printf("%d ", arr[i]);
	}
	return 0;
}
